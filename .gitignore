/CLAUDE.md
/protocol_weeks_1_2_v2.md
.pdf

# Run outputs. Results live in a versioned Kaggle Dataset, not in git
# (CLAUDE.md §3). runs/LEDGER.md is the exception -- it is the compute appendix.
runs/*
!runs/LEDGER.md

# Cached datasets, built once by scripts/prepare_data.py and uploaded as a
# Kaggle Dataset. Never committed.
data/

# Kaggle upload bundles, rebuilt on demand by scripts/package_for_kaggle.py
dist/

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/

# Staged Kaggle uploads, rebuilt by scripts/package_for_kaggle.py
kaggle_upload/

# The manuscript. Only the compiled PDF is pushed -- the LaTeX sources, the
# vendored TMLR style files, the copied figures and the build intermediates are
# all reproducible from this repo (`bash paper/build.sh`), so committing them
# would add churn without adding information.
paper/*
!paper/recycling_the_living.pdf
